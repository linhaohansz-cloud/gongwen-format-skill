---
name: 国企公文格式
description: >
  生成 / 排版 / 修改符合 GB/T 9704（党政机关公文格式）标准的国企公文 Word 文档。
  当用户提供公文原文并要求「按公文格式 / 标准版 / 排版 / 修改」，或要求生成一份
  标准格式的公文（标题、主送单位、一级/二级/三级标题、正文等）时使用。
  核心做法：复制用户提供的「标准版模板」docx，仅替换正文内容，100% 继承其页面
  设置与页脚页码。严格遵循已沉淀的格式规范（字体、字号、边距、行距、空行、倒梯形
  标题、小标题不分页等），执行中遇到任何不确定之处必须先向用户确认。
triggers:
  - 国企公文
  - 公文格式
  - 公文排版
  - 公文修改
  - 公文生成
  - 党政机关公文
  - GB/T 9704
  - 标准版模板
  - 排版公文
version: 1.0.0
license: MIT
metadata:
  version: "1.0.0"
  category: document-processing
  author: WorkBuddy
  sources:
    - "GB/T 9704-2012 党政机关公文格式"
---

# 国企公文格式

将任意公文原文 / 主题，排版成符合 **GB/T 9704 党政机关公文格式** 的标准 Word 文档。
本 skill 沉淀了一套经过实战验证的标准工作流，必须**严格执行**。

---

## ⚠️ 核心纪律（必须遵守）

1. **以模板为唯一权威**：所有格式来自用户指定的「标准版模板」docx，不擅自发明字体/边距/行距/结构。
2. **不会就问**：执行中遇到任何不确定、不会、或规范未覆盖之处，**必须先向用户确认，不得自行臆测**。
3. **一字不漏**：正文内容忠实于用户原文（或用户授权撰写的主题），不增删、不改写原意。
4. **交付物**：产出可直接用 Word 打开的 `.docx` 文件。

---

## 标准工作流（每次生成必走）

### 第 1 步：确认模板
- 取得用户的「标准版模板」docx 路径（默认参考：`C:\Users\AW\OneDrive\codex\codex公文格式\公文格式标准版.docx`）。
- 若用户本次未提供模板，**先问清楚路径**，不要凭空生成。
- 模板特征：采用**直接格式**（每个 run/paragraph 自带 rPr/pPr，不引用 pStyle），可 100% 继承页面与页脚。

### 第 2 步：准备内容 JSON
- 内容写成 JSON 文件，避免 Python 源码里中文直引号 `"` `"` 与字符串定界符冲突。
- 结构：`[[kind, text], ...]`，`kind ∈ {title, date, zhusong, body, h1, h2, h3}`。
  - `title` 标题（可多行，每行一个数组项 → 倒梯形排布，第一行最长）
  - `date`  日期副标题，例：`(2026 年 08 月 16 日)`
  - `zhusong` 主送单位，例：`主送单位：市体育局、市商务局`
  - `body`  正文段落（仿宋、两端对齐、首行缩进 2 字符）
  - `h1` 一级标题（`一、二、三…`，黑体加粗）
  - `h2` 二级标题（`（一）（二）…`，楷体）
  - `h3` 三级标题（`（1）（2）…`，仿宋）
- `date` 之后脚本会自动插入一个**空行**，无需在 JSON 里写空项。

### 第 3 步：生成文档
运行 `scripts/gen_gongwen.py`：

```bash
python scripts/gen_gongwen.py \
  --template "公文格式标准版.docx" \
  --content "content.json" \
  --output "输出文件.docx"
```

脚本做的事：复制模板 → 解包 → 仅清空 `word/document.xml` 里 body 的 `<w:p>`，保留末位 `<w:sectPr>` 与页脚引用 → 按 JSON 重建段落（精确套用下表格式）→ 重新打包。
**这样可 100% 继承标准版的页面设置与页脚页码。**

### 第 4 步：验证（必做）
运行 `scripts/verify_gongwen.py` 对照验证清单自动核对：

```bash
python scripts/verify_gongwen.py "输出文件.docx"
```

逐项确认：页面边距、每个段落字体/字号/对齐/缩进/行距、日期后空行、标题非粗体、一级标题粗体、正文首行缩进 2 字符+两端对齐、页脚 `— PAGE —` 宋体、ZIP 完整性。

### 第 5 步：交付
- 交付 `.docx`；若原文件被 Word 占用导致无法覆盖，脚本会生成 `xxx_修正.docx` 并告知用户关闭 Word 后重试。
- 用 `open_result_view` 展示，用 `deliver_attachments` 交付。

---

## 格式规范（与标准版模板 100% 一致）

### 页面设置（sectPr / w:pgMar，单位 twips，1 inch = 1440 twips）
| 参数 | 值 | 换算 |
|------|-----|------|
| 纸张 | A4 (210×297mm) | — |
| 上边距 | 2098 | 37mm |
| 下边距 | 1984 | 35mm |
| 左边距 | 1531 | 27mm |
| 右边距 | 1531 | 27mm |
| 页眉 | 850 | 约 15mm (1.5cm) |
| 页脚 | 1531 | 27mm (2.7cm) |
| gutter | 0 | — |

**版心**：标准每页 **22 行 × 26 字**；可增减 1–2 行但要调整行距、**保持版心高度不变**。

### 字体 / 字号 / 对齐 / 缩进（sz 单位为 half-point，`sz=44`=22pt=二号）
| 元素 | 字体 | sz | 字号 | 对齐 | 首行缩进 | 加粗 |
|------|------|-----|------|------|----------|------|
| 标题（可两行） | 方正小标宋简体 | 44 | 二号 | 居中 | 无 | **否**（非粗体） |
| 日期副标题 | 楷体_GB2312 | 32 | 三号 | 居中 | 无 | 否 |
| 空行 | （无 run） | — | — | — | — | keepNext + line=560 exact，无文字 |
| 主送单位 | 仿宋_GB2312 | 32 | 三号 | 左对齐 | 无 | 否 |
| 正文 body | 仿宋_GB2312 | 32 | 三号 | 两端对齐 | firstLine=640（2 字符） | 否，含 rSpacing=8 |
| 一级标题 h1 | 黑体 | 32 | 三号 | 左对齐 | 无 | **是**（粗体），含 rSpacing=8 |
| 二级标题 h2 | 楷体_GB2312 | 32 | 三号 | 左对齐 | 无 | 否，含 rSpacing=8 |
| 三级标题 h3 | 仿宋_GB2312 | 32 | 三号 | 左对齐 | 无 | 否，含 rSpacing=8 |

### 行距（统一）
- 所有段落：`w:spacing w:line="560" w:lineRule="exact"`（固定 28 磅）。
- 所有段落 pPr 均带 `<w:keepNext/>`（标题、日期、主送、各级标题、正文一致）。
- 正文段落额外带 `<w:keepLines w:val="0"/>` 与 `<w:widowControl/>`。

### 页脚 / 页码
- 格式：`— PAGE —`（左右各一条一字线，中间页码域）。
- 字体：宋体；`sz=28`（14pt）；`jc` 右对齐；行距 `line=280 lineRule=exact`（14 磅）。
- 页码为 `PAGE` 域，不要写死数字。

### 文档结构顺序（必须遵循，含关键空行）
1. 标题（方正小标宋，可拆两行，居中，**倒梯形排布**：第一行最长，逐行递减）
2. 日期副标题（楷体，居中）
3. **空一行**（仅 keepNext + line=560 exact，无文字）—— ⚠️ 日期后必须空一行
4. 主送单位（仿宋，左对齐）
5. 引言/导语正文（仿宋，两端对齐，首行缩进 2 字符）
6. 一级标题（黑体加粗）→ 二级标题（楷体）→ 三级标题（仿宋）→ 正文，按需嵌套
7. 结尾一般无落款/印章区；如用户要求落款再另行处理

### 分页控制
- **小标题尽量不要分页**：二级 `（一）`、三级 `（1）` 等与其引领的正文段应避免被分页断开。
- 实现：为各级标题设置 `<w:keepNext/>`，确保标题与紧随其后的正文首段同页。
- 长正文段自然延续至下一页可接受，但避免同一"小点"（标题+其起始正文）被拆开。

---

## 依赖
- Python 3 + `lxml`（`pip install lxml`）。生成器用 `lxml.etree` 精确构造/重写 OpenXML。
- 标准库 `zipfile` 用于复制+重新打包 docx。

## 文件
- `scripts/gen_gongwen.py` — 生成器（复制模板、替换正文）
- `scripts/verify_gongwen.py` — 验证器（对照上表自动核对）
- `references/format_spec.md` — 完整格式规范（与本文一致，便于单独查阅）
